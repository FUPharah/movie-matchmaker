<h1><%= @movie["Title"] %></h1>
<div class="movie-card" style="margin: 0 auto; text-align: center;">
  <% if @movie["Response"] == "True" %>
    <div class="movie-poster">
      <div class="front">
        <img src="<%= @movie["Poster"] %>" alt="<%= @movie["Title"] %>">
      </div>
      <div class="back">
        <p><%= @movie["Plot"] %></p>
      </div>
    </div>
    <div class="movie-info">
      <h3><%= @movie["Title"] %></h3>
      <p>Genre: <%= @movie["Genre"] %></p>
      <p>Release Date: <%= @movie["Released"] %></p>
      <%# <p>Mood: <%= @movie["Mood"] %></p>

      <%= link_to "Add to Favorite", add_favorite_movie_path(@movie["imdbID"]), method: :post, class: "btn btn-primary" %>
      <%= link_to "Add to Watchlist", add_watchlist_movie_path(@movie["imdbID"]), method: :post, class: "btn btn-primary" %>
    </div>
  <% else %>
    <div class="movie-info">
      <p><%= @movie["Error"] %></p>
    </div>
  <% end %>
</div>
<%= link_to "Back to Home", root_path, class: "btn btn-secondary" %>
